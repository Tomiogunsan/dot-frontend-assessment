import Drawer from "../../../shared/Drawer";

const AddToCart = ({ onClose }: { onClose: () => void }) => {
  return <Drawer onClose={onClose} header="Add To Cart">AddToCart</Drawer>;
};

export default AddToCart;
